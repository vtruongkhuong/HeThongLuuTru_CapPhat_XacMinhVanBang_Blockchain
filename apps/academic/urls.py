from django.urls import path

app_name = 'academic' # (Tùy chọn) Khai báo namespace cho app

urlpatterns = [
    # Bạn sẽ viết các đường dẫn của app academic vào đây sau, ví dụ:
    # path('', views.index, name='index'),
]